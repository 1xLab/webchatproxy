diff --git a/src/deepseek/updates.ts b/src/deepseek/updates.ts
index 4b5742f..b17ae16 100644
--- a/src/deepseek/updates.ts
+++ b/src/deepseek/updates.ts
@@ -88,7 +88,7 @@ export async function* iterDeepSeekUpdates(response: Response): AsyncGenerator<D
       continue;
     }
 
-    const isContentAppend = data.p === "response/fragments/-1/content" && data.o === "APPEND";
+    const isContentAppend = data.p === "response/fragments/-1/content" && typeof data.v === "string";
     const isRootAppend = data.p === undefined && data.o === "APPEND" && data.v !== undefined;
     const isBareString = data.p === undefined && data.o === undefined && typeof data.v === "string";
     if (isContentAppend || isRootAppend || isBareString) {
